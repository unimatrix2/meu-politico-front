import React, { useState, useEffect } from 'react';
import api from '../../../services/api.service';

const PoliticoTable = () => {
    const [userPoliticos, setUserPoliticos] = useState(false);

    return (
        <h1>Bateu em políticos</h1>
    )
}

export default PoliticoTable